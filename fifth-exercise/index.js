let height = 165

let height_meters = 1.65

let weight = 75.5

let height_max = Math.ceil(height_meters);

let weight_min = Math.floor(weight)

let equals = Number.MAX_VALUE + 1 === Number.MAX_VALUE;

console.log(height, height_meters, height_max,weight, weight_min,equals)